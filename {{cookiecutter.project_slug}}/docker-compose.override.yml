version: '3.7'
services: 
    pgadmin:
        labels:
            - "traefik.http.routers.pgadmin.rule=Host(`pgadmin.${DOMAIN}`)"
    proxy:
        command: 
            - --providers.docker
        ports:
            - 80:80